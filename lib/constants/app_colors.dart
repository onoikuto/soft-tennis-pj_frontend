/// アプリ全体で使用する色の定数
/// 
/// シンプルなカラーパレットを定義します。
class AppColors {
  AppColors._(); // インスタンス化を防ぐ

  // 背景色
  static const Color backgroundLight = Color(0xFFFCFCFC);
  static const Color backgroundDark = Color(0xFF000000);
  static const Color backgroundIvory = Color(0xFFFDFDFB);

  // テキスト色
  static const Color textPrimary = Color(0xFF333333);
  static const Color textSecondary = Color(0xFF7F7F7F);
  static const Color textMuted = Color(0xFF888888);
  static const Color textLight = Color(0xFF999999);

  // ボーダー色
  static const Color border = Color(0xFFE0E0E0);
  static const Color borderLight = Color(0xFFEFEFEF);

  // アクセント色
  static const Color primary = Color(0xFF4C6B70);
  static const Color primaryDark = Color(0xFF000000);

  // 入力フィールド背景色
  static const Color inputBackground = Color(0xFFF9F9F9);

  // 白
  static const Color white = Color(0xFFFFFFFF);
}
